Debug记录：

1.MDMA一次读取超过65536字节时，耗时异常，且读出数据错误。

解决办法：限制DMA一次读取字节数不超过65536， 见HAL_MDMA_Start_IT注释。

2.MDMA读取数据时，buffer前16字节未正常写入数据，buffer地址:0xXXXX5570。

解决办法：未查明原因，怀疑与数据对齐有关，将buffer 进行32字节对齐后正常。

/* cahce影响， DMA读取后建议InvalidDcache */

3.擦除扇区失败。

解决办法：移植的扇区擦除函数传入的是扇区ID，非扇区地址，应x4096。